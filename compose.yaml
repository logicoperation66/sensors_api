version: '3.8'

services:
  esp-api:
    build: .
    container_name: esp-api
    restart: unless-stopped
    ports:
      - "192.168.3.178:20378:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080